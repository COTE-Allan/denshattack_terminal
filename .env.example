# Copy this file to .env and fill in your endpoint.
# Never commit .env itself.

# No PUBLIC_ prefix: this stays server-side and never reaches the browser bundle.
WP_GRAPHQL_URL=https://cms.yourdomain.com/graphql
