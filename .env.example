# Copy this file to .env and fill in your endpoint.
# Never commit .env itself.

# PUBLIC_ prefix required: this ships in the browser bundle, every page fetches wordpress live.
PUBLIC_WP_GRAPHQL_URL=https://cms.yourdomain.com/graphql
